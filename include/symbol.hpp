#pragma once
#include <string>

namespace ILC {
namespace symbol {
using namespace std;
enum symbol_t { S, A, B, PLUS, TIMES, NUM };
// string symbols_string[] = {"S", "A", "B", "+", "*", "#"};
}; // namespace symbol
using namespace symbol;
} // namespace ILC